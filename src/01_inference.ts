let count = 0;
let nam = "Poorneshsvavmsvavs";
let maybe: string | number;

let x = Math.random() > 0.6 ? "ghdsgdhgsd" : 67;

function addTwoNumbers(a: number, b: number): number {
  return a + b;
}

console.log(addTwoNumbers(0, 7));

//Inference - the compiler's ability to automatically figure out a variable's type from its assigned value
